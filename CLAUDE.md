# Rules

- DO NOT EVER EVER TOUCH SIMULATORS. No booting, installing, uninstalling, launching, or UI-automating iOS Simulators. Never run `xcrun simctl` (or AppleScript against the Simulator app) for any reason, including "just to verify" or "just to preview." If visual verification is needed, ask the user to do it or add a SwiftUI `#Preview`.
